import { ChildrenType } from "@/utils/types";

const AuthLayout = ({ children }: ChildrenType) => {
    return (
        <>
            {children}
        </>
    )
}

export default AuthLayout;